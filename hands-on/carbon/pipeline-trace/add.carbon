// Pipeline trace source (issue #27): one function traced from tokens to an
// object file. See docs/evidence/pipeline-trace.md.
package PipelineTrace;

fn Add(a: i32, b: i32) -> i32 {
  return a + b;
}
